# ChatMind - 技术架构与开发方案

> 版本: v0.2  
> 日期: 2026-05-16

---

## 一、技术选型

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     展示层 (Presentation)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  导入页面    │  │  分析仪表盘  │  │  AI 对话 / 报告页    │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│                        Vue 3 + TypeScript                    │
├─────────────────────────────────────────────────────────────┤
│                     应用层 (Application)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  导入引擎    │  │  分析引擎    │  │  AI Agent 调度器     │  │
│  │  (Parser)   │  │  (Analyzer) │  │  (Agent)            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                     数据层 (Data)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  IndexedDB  │  │  SQLite     │  │  文件系统 (Electron) │  │
│  │  (Web 版)   │  │  (桌面版)   │  │                     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                     计算层 (Compute)                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Web Worker │  │  WebAssembly│  │  本地 AI (Ollama)   │  │
│  │  (主线程卸载)│  │  (分词/词典)│  │  (可选)             │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层级 | 技术 | 理由 |
|------|------|------|
| 前端框架 | Vue 3 + TypeScript | 生态成熟，ChatLab 也采用，参考代码多 |
| UI 组件库 | Naive UI | 数据密集型应用，暗色主题支持完善 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| 可视化 | ECharts + vue-echarts | 时间序列、双轴对比成熟，暗色主题适配 |
| 构建工具 | Vite | 快速冷启动，Vue 生态标配 |
| 数据库 | IndexedDB + Dexie.js | 浏览器本地存储，百万条数据可承受 |
| 分词/情感 | 本地规则引擎 | 情感词典 + 表情映射 + 标点特征，零成本 |
| AI 接入 | Fetch API（OpenAI 兼容格式） | 统一接口，支持 8 家国产模型 + Ollama + 自定义端点 |

---

## 二、目录结构

```
chatmind/
├── doc/                           # 文档
│   ├── 01-产品需求文档(PRD).md
│   ├── 02-技术架构与开发方案.md
│   ├── 03-数据格式规范.md
│   ├── 04-AI-分析与-Prompt-设计.md
│   ├── 05-交互设计流程.md
│   ├── 06-变更日志.md
│   └── 07-功能优化与完善方向.md
│
├── src/
│   ├── ai/                        # AI 相关
│   │   ├── client.ts             # AI 客户端（流式调用、超时重试）
│   │   ├── providers.ts          # 8 家模型预设配置 + Ollama
│   │   ├── ollama.ts             # Ollama 本地服务检测
│   │   ├── sanitizer.ts          # AI 脱敏工具
│   │   └── tools/                # Function Calling
│   │       ├── definitions.ts    # 工具定义
│   │       └── executor.ts       # 工具执行器
│   │
│   ├── analyzers/                 # 分析引擎
│   │   ├── emotion.ts            # 情绪分析（规则引擎）
│   │   ├── statistics.ts         # 基础统计（消息量/延迟/时段/长度）
│   │   ├── word-frequency.ts     # 词频/日历/延迟分布/长度趋势/情绪分布
│   │   ├── relationship-score.ts # 关系健康度五维评分
│   │   ├── danger-signals.ts     # 危险信号检测
│   │   ├── relationship-stage.ts # 关系阶段判定（规则引擎）
│   │   ├── emotion-dynamics.ts   # 情绪感染度/修复时间/对抗模式
│   │   ├── keyword-track.ts      # 关键词追踪
│   │   ├── time-shift.ts         # 聊天时段迁移分析
│   │   └── event-markers.ts      # 自动事件检测
│   │
│   ├── components/                # 组件
│   │   ├── AppLayout/            # 侧边栏布局
│   │   ├── ChatImport/           # 导入组件
│   │   ├── IdentitySelector/     # 身份选择弹窗
│   │   └── ModelConfig/          # 模型配置弹窗
│   │
│   ├── db/                        # 数据库
│   │   └── schema.ts             # IndexedDB 表结构（v2）
│   │
│   ├── parsers/                   # 导入解析器
│   │   ├── index.ts              # 解析器注册
│   │   ├── weflow-json.ts        # WeFlow JSON
│   │   ├── weflow-csv.ts         # WeFlow CSV
│   │   └── generic-txt.ts        # 通用 TXT（3 种格式自动识别）
│   │
│   ├── stores/                    # Pinia Stores
│   │   ├── ai.ts                 # AI 对话状态（含 Prompt 构建）
│   │   ├── identity.ts           # 身份识别状态
│   │   ├── import.ts             # 导入状态
│   │   ├── model-config.ts       # 多模型配置
│   │   ├── session.ts            # 会话/消息数据（含事件 CRUD）
│   │   └── theme.ts              # 主题状态
│   │
│   ├── types/                     # TypeScript 类型
│   │   ├── analysis.ts
│   │   ├── identity.ts
│   │   └── message.ts
│   │
│   ├── utils/                     # 工具函数
│   │   ├── chart-theme.ts        # ECharts 暗色主题适配
│   │   ├── date.ts               # 日期格式化
│   │   ├── demo-data.ts          # 示例数据生成
│   │   ├── emotion-dict.ts       # 情感词典
│   │   └── export.ts             # 报告导出（html2canvas + jsPDF）
│   │
│   ├── views/                     # 页面视图
│   │   ├── AiChatView.vue        # AI 分析师
│   │   ├── AnalysisView.vue      # 深度分析（词云/日历/延迟/长度/情绪/对比）
│   │   ├── DashboardView.vue     # 分析仪表盘（健康度/危险信号/阶段判定）
│   │   ├── ImportView.vue        # 导入页
│   │   ├── MessageListView.vue   # 聊天记录列表（身份校准/情绪修正）
│   │   ├── ReportExportView.vue  # 报告导出
│   │   └── TimelineView.vue      # 情绪时间轴（事件标记/里程碑）
│   │
│   ├── App.vue                    # 根组件（暗色主题/CSS 变量）
│   └── main.ts                    # 入口
│
├── public/                        # 公共资源
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

> **未来规划（未实现）**：Web Worker 解析大文件、Electron 桌面端（SQLite + WeFlow API 直连）、Vue Composables 抽象公共逻辑。

---

## 三、核心模块设计

### 3.1 导入引擎 (Parser)

#### 3.1.1 解析器接口

```typescript
// src/parsers/index.ts
interface Parser<T = any> {
  name: string;
  extensions: string[];           // 支持的文件扩展名
  mimeTypes: string[];            // 支持的 MIME 类型
  
  // 解析文件
  parse(file: File): Promise<ParseResult<T>>;
  
  // 检测是否能解析（预览前 1KB 内容）
  canParse(sample: string): boolean;
}

interface ParseResult<T> {
  format: string;                 // 解析格式标识
  rawData: T;                     // 原始解析数据
  messages: RawMessage[];         // 标准化后的消息
  participants: RawParticipant[]; // 参与者信息
  metadata: ParseMetadata;        // 元数据
}

interface RawMessage {
  id: string;
  timestamp: number;
  senderName: string;             // 显示昵称
  senderWxid?: string;            // 微信 ID（可能缺失）
  content: string;
  type: 'text' | 'image' | 'voice' | 'video' | 'file' | 'system';
}

interface RawParticipant {
  name: string;
  wxid?: string;
  avatar?: string;
  isSystem?: boolean;
}
```

#### 3.1.2 解析流程

```
[用户拖拽文件]
     ↓
[读取文件前 1KB]
     ↓
[遍历注册器，调用 canParse() 匹配]
     ↓
[匹配成功 → 调用 parse() 完整解析]
     ↓
[标准化为 RawMessage[] + RawParticipant[]]
     ↓
[展示预览（前 20 条消息）]
     ↓
[等待用户确认身份]
     ↓
[写入数据库]
```

### 3.2 身份识别系统 (Identity)

#### 3.2.1 核心逻辑

```typescript
// src/stores/identity.ts
interface IdentityState {
  // 用户确认的身份映射
  confirmedMapping: Map<string, 'self' | 'other'>;  // name/wxid -> role
  
  // 曾用名记录
  aliasHistory: Alias[];
  
  // 自动推断的结果（供参考）
  inferredSelf: string[];
}

// 核心函数
function resolveSenderIdentity(
  msg: Message,
  state: IdentityState
): 'self' | 'other' | 'unknown' {
  // 1. 优先用 wxid 匹配
  if (msg.senderWxid && state.confirmedMapping.has(msg.senderWxid)) {
    return state.confirmedMapping.get(msg.senderWxid)!;
  }
  
  // 2. 用当前昵称匹配
  if (state.confirmedMapping.has(msg.senderName)) {
    return state.confirmedMapping.get(msg.senderName)!;
  }
  
  // 3. 用曾用名时间线匹配
  for (const alias of state.aliasHistory) {
    if (alias.name === msg.senderName && 
        msg.timestamp >= alias.startTime &&
        (!alias.endTime || msg.timestamp <= alias.endTime)) {
      return state.confirmedMapping.get('self')!;
    }
  }
  
  return 'unknown';
}
```

#### 3.2.2 辅助推断规则

```typescript
// src/analyzers/identity-inference.ts

const inferenceRules = [
  // 规则1：系统消息中的"你"
  {
    name: 'system-message-self',
    test: (msg: Message) => {
      if (msg.type !== 'system') return false;
      const selfIndicators = ['你已添加', '你邀请', '你修改', '你撤回'];
      return selfIndicators.some(ind => msg.content.includes(ind));
    },
    infer: (msg: Message) => msg.senderName,  // 这条系统消息的发送者标识就是"我"
  },
  
  // 规则2：文件传输助手
  {
    name: 'file-transfer-helper',
    test: (msg: Message, context: Message[]) => {
      // 在"文件传输助手"对话中，非助手方的发言者就是"我"
      const isFTChat = context.some(m => 
        m.senderName === '文件传输助手'
      );
      return isFTChat && msg.senderName !== '文件传输助手';
    },
    infer: (msg: Message) => msg.senderName,
  },
];
```

### 3.3 数据库设计

#### 3.3.1 Web 版 (IndexedDB via Dexie)

```typescript
// src/db/schema.ts
import Dexie, { Table } from 'dexie';

export interface DbMessage {
  id: string;                     // 唯一 ID
  sessionId: string;              // 所属会话
  senderId: string;               // 内部参与者 ID
  isSelf: boolean;                // 是否用户本人
  timestamp: number;
  content: string;
  type: MessageType;
  emotion?: EmotionLabel;         // 预计算的情绪标签
  emotionScore?: number;          // 情绪置信度
  emotionMethod?: 'rule' | 'user'; // 标注方式：规则 / 用户修正
  wordCount: number;
}

export interface DbParticipant {
  id: string;                     // 内部 ID
  sessionId: string;
  wxid?: string;
  names: string[];                // 所有出现过的名称
  role: 'self' | 'other' | 'unknown';
  messageCount: number;
  firstSeen: number;
  lastSeen: number;
}

export interface DbSession {
  id: string;
  name: string;                   // 会话名称（对方昵称/群名）
  type: 'private' | 'group';
  participantIds: string[];
  importedAt: number;
  messageCount: number;
  timeRange: [number, number];    // [first, last]
  sourceFormat: string;           // 导入格式标识
  sourceFileName: string;         // 原始文件名
}

export interface DbEvent {
  id: string;
  sessionId: string;
  date: string;                   // YYYY-MM-DD
  type: string;                   // 事件类型
  title: string;
  description: string;
  severity: 'low' | 'medium' | 'high';
  isAuto: boolean;                // true: 自动检测, false: 用户手动添加
  createdAt: number;
}

export class ChatMindDB extends Dexie {
  messages!: Table<DbMessage>;
  participants!: Table<DbParticipant>;
  sessions!: Table<DbSession>;
  events!: Table<DbEvent>;

  constructor() {
    super('ChatMindDB');
    this.version(1).stores({
      messages: 'id, [sessionId+timestamp], senderId, timestamp, emotion',
      participants: 'id, [sessionId+names], role',
      sessions: 'id, importedAt',
    });
    this.version(2).stores({
      events: 'id, [sessionId+date], date',
    });
  }
}
```

#### 3.3.2 索引策略

```
messages 表:
  - 主键: id
  - 复合索引: [sessionId + timestamp] → 按会话分页查询
  - 单索引: senderId → 统计某人发言
  - 单索引: timestamp → 时间范围查询
  - 单索引: emotion → 情绪筛选
```

### 3.4 分析引擎 (Analyzer)

#### 3.4.1 基础统计

```typescript
// src/analyzers/statistics.ts
interface StatisticsResult {
  totalMessages: number;
  selfMessages: number;
  otherMessages: number;
  selfRatio: number;              // 我占总消息比例
  
  // 时间分布
  hourlyDistribution: number[];   // 24小时，每小时消息数
  dailyDistribution: Map<string, number>; // 日期 -> 消息数
  
  // 长度统计
  avgLength: { self: number; other: number };
  maxLength: { self: number; other: number };
  
  // 回复延迟
  replyDelays: number[];          // 每次回复的延迟秒数
  avgReplyDelay: number;
}

function calculateStatistics(messages: DbMessage[]): StatisticsResult {
  // 按时间排序
  const sorted = [...messages].sort((a, b) => a.timestamp - b.timestamp);
  
  // 计算回复延迟
  const delays: number[] = [];
  for (let i = 1; i < sorted.length; i++) {
    const prev = sorted[i - 1];
    const curr = sorted[i];
    if (prev.isSelf !== curr.isSelf) {
      delays.push(curr.timestamp - prev.timestamp);
    }
  }
  
  // ... 其他计算
}
```

#### 3.4.2 情绪分析

```typescript
// src/analyzers/emotion.ts

type EmotionLabel = 'positive' | 'negative' | 'neutral' | 'angry' | 'sad' | 'affectionate';

interface EmotionResult {
  label: EmotionLabel;
  score: number;                  // 置信度 0-1
  method: 'rule' | 'ai';          // 分析方法
}

// 规则层：快速本地分析
function ruleBasedEmotion(text: string): EmotionResult | null {
  // 正面词库
  const positiveWords = ['开心', '喜欢', '爱你', '谢谢', '哈哈', '棒', '好'];
  // 负面词库
  const negativeWords = ['烦', '讨厌', '生气', '失望', '难过', '滚', '无语'];
  // 愤怒词库
  const angryWords = ['滚', '操', '他妈', '有病', '傻逼', '恶心'];
  
  let score = 0;
  let matched = false;
  
  for (const w of positiveWords) {
    if (text.includes(w)) { score += 1; matched = true; }
  }
  for (const w of negativeWords) {
    if (text.includes(w)) { score -= 1; matched = true; }
  }
  for (const w of angryWords) {
    if (text.includes(w)) { return { label: 'angry', score: 0.9, method: 'rule' }; }
  }
  
  if (!matched) return null;
  
  return {
    label: score > 0 ? 'positive' : score < 0 ? 'negative' : 'neutral',
    score: Math.min(Math.abs(score) * 0.3 + 0.5, 0.9),
    method: 'rule',
  };
}

// AI 层：批量聚合分析（调用大模型）
async function aiBatchEmotion(
  messages: DbMessage[],
  aiClient: AiClient
): Promise<EmotionResult[]> {
  const prompt = `分析以下聊天记录的情绪倾向，返回 JSON 数组：
  每条记录包含：label(positive/negative/neutral/angry/sad/affectionate), score(0-1)
  
  聊天记录：
  ${messages.map((m, i) => `${i}. [${m.isSelf ? '我' : '对方'}] ${m.content}`).join('\n')}
  `;
  
  // ... 调用 AI
}
```

### 3.5 AI Agent 设计

#### 3.5.1 Function Calling（文本标记式实现）

为兼容所有 OpenAI 兼容接口的国产模型（而非仅原生支持 `tools` 参数的模型），采用文本标记式实现：

```typescript
// src/ai/tools/definitions.ts

export const tools = [
  {
    name: 'query_messages',
    description: '查询指定时间范围内的聊天记录',
    parameters: { startDate: string, endDate: string, limit?: number },
  },
  {
    name: 'query_emotion_trend',
    description: '查询情绪趋势数据',
    parameters: { period?: string, granularity?: string },
  },
  {
    name: 'query_statistics',
    description: '查询统计指标',
    parameters: { metric?: string },
  },
  {
    name: 'search_keywords',
    description: '搜索关键词在聊天记录中的出现情况',
    parameters: { keyword: string, limit?: number },
  },
];

// AI 在回复中插入工具调用标记：
// [TOOL_CALL: {"name": "query_statistics", "parameters": {"metric": "reply_delay"}}]
```

执行流程（两轮对话）：
1. 第一轮：用户提问 → AI 回复（可能包含 `[TOOL_CALL: {...}]` 标记）
2. 系统检测到工具调用 → 执行 `ToolExecutor` → 获取结果
3. 第二轮：将工具结果作为上下文再次发送给 AI → AI 生成最终回答

#### 3.5.2 AI 回答可溯源

系统 Prompt 引导 AI 在引用具体消息时使用 `[MSG:YYYY-MM-DD]` 格式：

```
当你引用聊天记录中的具体日期时，请使用 [MSG:YYYY-MM-DD] 格式标注，
例如："3月15日你们的交流出现了明显的负面情绪 [MSG:2024-03-15]"。
```

前端解析：
```typescript
// src/views/AiChatView.vue
function formatMessageContent(content: string): string {
  return content
    .replace(/\[MSG:(\d{4}-\d{2}-\d{2})\]/g,
      '<a class="msg-trace-link" href="#" data-date="$1">📎 查看 $1 记录</a>')
    .replace(/\n/g, '<br>');
}
```

用户点击链接后弹出 Modal，展示该日期的完整聊天记录时间轴。

#### 3.5.3 AI 客户端封装

```typescript
// src/ai/client.ts

interface AiClientOptions {
  timeout?: number;   // 默认 30000ms
  retries?: number;   // 默认 1 次
}

class AiClient {
  async *chatStream(
    messages: ChatMessage[],
    config: ModelConfig,
    signal?: AbortSignal
  ): AsyncGenerator<string> {
    // Fetch API + ReadableStream 解析 SSE
    // Ollama 自动跳过 Authorization header
    // AbortController 实现超时控制
  }
}
```

---

## 四、性能优化策略

### 4.1 大数据处理

| 场景 | 策略 | 实现 |
|------|------|------|
| 导入大文件 | 分块解析 + Web Worker | 每次读取 10MB，Worker 中解析 |
| 消息列表渲染 | 虚拟滚动 | vue-virtual-scroller |
| 图表渲染 | 数据降采样 | 时间轴数据点超过 1000 时抽样 |
| 查询统计 | 预计算 + 缓存 | 导入时计算基础指标存入数据库 |
| AI 分析 | 批量而非逐条 | 每 50 条消息一批调用 AI |

### 4.2 内存管理

```typescript
// 导入完成后释放原始文件数据
function afterImport() {
  rawFileData = null;  // 允许 GC
  // 只保留标准化后的数据库数据
}

// 长对话分页加载
async function loadMessages(sessionId: string, page: number) {
  return db.messages
    .where('[sessionId+timestamp]')
    .between([sessionId, start], [sessionId, end])
    .offset(page * PAGE_SIZE)
    .limit(PAGE_SIZE)
    .toArray();
}
```

---

## 五、安全与隐私

### 5.1 数据安全

```
1. 本地存储: 所有聊天记录存储在 IndexedDB/SQLite，不上传
2. 内存清理: 页面刷新/关闭后内存数据消失
3. 导出加密: 分析报告导出时可选密码保护
```

### 5.2 AI 调用安全

```typescript
// src/ai/sanitizer.ts

// 脱敏函数：调用云端 AI 前清理敏感信息
function sanitizeForAI(messages: DbMessage[]): SanitizedMessage[] {
  return messages.map(m => ({
    ...m,
    // 替换具体昵称为 role 标识
    senderLabel: m.isSelf ? '用户A' : '用户B',
    // 移除可能的手机号、微信号
    content: m.content
      .replace(/\b1[3-9]\d{9}\b/g, '[手机号]')
      .replace(/wxid_[a-zA-Z0-9_]+/g, '[微信ID]'),
  }));
}
```

---

## 六、开发里程碑

### Phase 1: MVP ✅ 已完成

| 周次 | 任务 | 产出 |
|------|------|------|
| W1 | 项目搭建 + 数据库 + 导入解析 | 可运行的基础页面 |
| W2 | 身份选择系统 + 基础统计 | 身份识别正确 + 统计图表 |
| W3 | 情绪分析（规则层）+ 时间轴 | 情绪曲线图 |
| W4 | 对比视图 + 词云 | MVP 完整可演示 |

### Phase 2: AI 增强与关系分析 ✅ 已完成

| 周次 | 任务 | 状态 |
|------|------|------|
| W5 | AI 客户端接入 + 多模型配置 | ✅ 支持 8 家国产 + Ollama |
| W6 | AI Function Calling + 回答可溯源 | ✅ 文本标记式实现 |
| W7 | 关系健康度 + 危险信号 + 阶段判定 | ✅ 规则引擎版 |
| W8 | 情绪感染度 + 时段迁移 + 关键词追踪 | ✅ 深度分析页面 |
| W9 | 报告导出（PDF / 图片）| ✅ html2canvas + jsPDF |

### Phase 3: 工程化与长期愿景 ⏳ 待推进

| 周次 | 任务 | 状态 |
|------|------|------|
| W10 | 单元测试覆盖（Vitest）| ⏳ 待开发 |
| W11 | ECharts 按需加载 / 代码分割 | ⏳ 待开发 |
| W12+ | Electron 桌面端（可选）| ⏳ 未来规划 |

---

## 七、测试策略

```
单元测试 (Vitest)
  ├─ parsers/ - 各种格式的解析正确性
  ├─ analyzers/ - 统计计算准确性
  └─ identity/ - 身份识别逻辑

集成测试 (Playwright)
  ├─ 导入流程端到端
  ├─ 身份选择交互
  └─ 图表渲染

性能测试
  ├─ 10万条消息导入时间 < 10s
  ├─ 100万条消息查询不卡顿
  └─ 内存占用 < 500MB
```
